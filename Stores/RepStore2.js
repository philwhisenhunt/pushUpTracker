import { store, view } from "@risingstack/react-easy-state";

const RepStore2 = store({
  "Push-outs": 0,
});

setReps(name, count) {
    this[name] = count;
},

get totalReps() {
    return (
        this
    )
}
